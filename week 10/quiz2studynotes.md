Project statement and objectives
Scientific and functional requirements
Structured methodology
Role of issue boards
Open, doing, review, done
tasks vs milestones

Separation on concerns -> team collaboration
Presentation layer : frontend
Business layer : backend
Data Layer : SQL (retrieves and stores data)
Static, Data (heavier logic for processing), API integration, folder structure
API, JSON
Restful APIs use GET, POST, PUT -> creates replaces a ressource in an URI, DELETE, PATCH -> performs partial update on ressourc
await fetch, await response.json()
internal vs external APIs
store API keys on .env files

Be desciptive with function and variable names
Code documentation
Group modules in a package with __init__.py files
object = state + actions
Use encapsulation for classes

SQL, API, CSV
Entity, Attribute, Relationship
primary key, foreign key 
Object modelling -> class diagram (JSON tree model)
SELECT column FROM table WHERE condition
Create, read, update, delete

1. Reproduce the issue 
2. Observe the evidence 
3. Isolate the cause 
4. Make a small fix 
5. Re-test the program
console, network, elements, sources
call stack, watch variables in IDE debugger

Plotly for interactive visualization
Data, visuals, narrative
Obvious comparison, one chart per idea
Separate saving raw data from plotting code

